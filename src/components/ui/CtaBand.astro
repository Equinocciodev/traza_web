---
/**
 * Banda de cierre con título, texto y dos CTA sobre azul marino, con motivo QR discreto.
 */
import Button from './Button.astro';
import QrMotif from './QrMotif.astro';
import { ctaHref, ctaVariant } from './cta';
import type { Cta } from '@/content/types';
import type { Locale } from '@/i18n';

interface Props {
  locale: Locale;
  title: string;
  body?: string;
  primaryCta: Cta;
  secondaryCta?: Cta;
  id?: string;
  class?: string;
}
const { locale, title, body, primaryCta, secondaryCta, id = 'cta-title', class: className } = Astro.props;
---

<!-- Motion: el revelado va en el contenedor interior (copia y acciones escalonadas); el fondo azul y el motivo QR
     decorativo quedan siempre pintados, así un scroll rápido nunca deja un hueco blanco antes del pie. -->
<section class:list={['cta-band', 'on-dark', className]} aria-labelledby={id}>
  <div class="cta-band__motif" aria-hidden="true"><QrMotif /></div>
  <div class="container cta-band__inner" data-reveal data-reveal-stagger>
    <div class="cta-band__copy">
      <h2 id={id} class="cta-band__title">{title}</h2>
      {body && <p class="cta-band__body">{body}</p>}
    </div>
    <div class="cta-band__actions cluster">
      <Button href={ctaHref(locale, primaryCta)} variant={ctaVariant(primaryCta, 'primary')} size="lg" icon="arrow-right">
        {primaryCta.label}
      </Button>
      {
        secondaryCta && (
          <Button href={ctaHref(locale, secondaryCta)} variant={ctaVariant(secondaryCta, 'secondary')} size="lg">
            {secondaryCta.label}
          </Button>
        )
      }
    </div>
  </div>
</section>

<style>
  .cta-band {
    position: relative;
    overflow: hidden;
    isolation: isolate;
    padding-block: clamp(3rem, 6vw, 5rem);
    background: var(--color-navy-900);
  }
  .cta-band__motif {
    position: absolute;
    z-index: -1;
    right: -9rem;
    top: -10rem;
    width: 20rem;
    height: 20rem;
    color: var(--color-white);
    opacity: 0.8;
    pointer-events: none;
  }
  @media (max-width: 59.99em) {
    .cta-band__motif {
      display: none;
    }
  }
  .cta-band__inner {
    display: grid;
    gap: var(--space-8);
    align-items: center;
  }
  @media (min-width: 60em) {
    .cta-band__inner {
      grid-template-columns: minmax(0, 3fr) minmax(0, 2fr);
    }
    .cta-band__actions {
      justify-content: flex-end;
    }
  }
  .cta-band__title {
    color: var(--color-white);
    font-size: var(--text-2xl);
    margin-bottom: var(--space-3);
  }
  .cta-band__body {
    max-width: 58ch;
    color: rgba(255, 255, 255, 0.82);
    margin: 0;
  }
  .cta-band__actions {
    gap: var(--space-4);
  }
</style>
