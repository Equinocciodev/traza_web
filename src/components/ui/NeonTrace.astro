---
import { withBase } from '@/i18n';
interface Props { variant?: 'orbit' | 'loop' | 'sweep' | 'connector'; tone?: 'dark' | 'light'; draw?: 'load' | 'view'; duration?: number }
const { variant = 'loop', tone = 'dark', draw } = Astro.props;
---
<img class:list={['neon-trace', `neon-trace--${variant}`, `neon-trace--${tone}`]} src={withBase('/images/brand/neon-loop-reference05.webp')} width="1536" height="1024" alt="" aria-hidden="true" decoding="async" loading={draw === 'load' ? 'eager' : 'lazy'} />
<style>
  .neon-trace { display:block; width:100%; height:100%; object-fit:contain; pointer-events:none; mask-image:linear-gradient(90deg,transparent,#000 8%,#000 92%,transparent),linear-gradient(0deg,transparent,#000 10%,#000 80%,transparent); mask-composite:intersect; }
  .neon-trace--sweep { object-position:center bottom; }
  .neon-trace--connector { object-position:center; }
</style>
